# CognoDB connection (never commit real credentials)
COGNODB_URI=bolt+s://db-e85d0a7b.databases.cognodb.com
COGNODB_USERNAME=cognodb
COGNODB_PASSWORD=your-password-from-cognodb-console

# Optional: set to true on first run to load seed data
SEED_DATA=true

# Server port (default 8080)
PORT=8080
